# MasterApp Documentation Pack

Copy the `architecture` and `api` folders into `MasterApp/docs/`.

```powershell
git add docs
git commit -m "Add architecture and API documentation"
git push origin main
```

GitHub renders Mermaid diagrams directly inside Markdown files.
